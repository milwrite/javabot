# Mason & Dixon Configuration
# Copy to .env and fill in values

# ============ Discord (New Bot Application) ============
# Create at: https://discord.com/developers/applications
MASON_DIXON_DISCORD_TOKEN=your_mason_dixon_bot_token
MASON_DIXON_CLIENT_ID=your_mason_dixon_client_id

# ============ Channel Configuration ============
# Channel where Bot Sportello operates (to monitor)
SPORTELLO_CHANNEL_ID=your_sportello_channel_id

# Channel for Mason & Dixon diagnostic output
DIAGNOSTICS_CHANNEL_ID=your_diagnostics_channel_id

# ============ MySQL Database (Railway) ============
# Create at: Railway Dashboard -> New Database -> MySQL
MYSQL_URL=mysql://user:password@host:port/mason_dixon

# ============ Sportello's PostgreSQL (Read-Only) ============
# Reuse from Bot Sportello's .env - for LISTEN/NOTIFY
SPORTELLO_DATABASE_URL=postgresql://user:password@host:port/database

# ============ OpenRouter API (Claude via OpenRouter) ============
# Reuse from Bot Sportello's .env - same key works for both bots
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key

# Model to use (default: anthropic/claude-sonnet-4)
# Options: anthropic/claude-opus-4, anthropic/claude-sonnet-4, anthropic/claude-haiku
# Or any OpenRouter model with tool use support (see: https://openrouter.ai/models?supported_parameters=tools)
MASON_DIXON_MODEL=anthropic/claude-haiku

# ============ Optional Configuration ============
# Analysis interval in milliseconds (default: 5000)
ANALYSIS_INTERVAL_MS=5000

# Log level: debug, info, warn, error
LOG_LEVEL=info

# Maximum events to buffer before forced analysis
MAX_EVENT_BUFFER=100
